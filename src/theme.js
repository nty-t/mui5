import { createTheme } from "@mui/material/";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#00bcd4",
      light: "skyblue",
    },
    secondary: {
      main: "#f50057",
    },
    otherColor: {
      main: "#999",
    },
  },
});
